"""CloudFormation template generators for ``ml-platform deploy aws``.

Unlike the CDK constructs (which require Node.js), these generators
produce raw CloudFormation JSON that is applied via ``boto3`` directly.
"""
