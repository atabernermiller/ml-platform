# Stateful Serving

The stateful serving path is for ML services that maintain mutable state and learn from delayed feedback — contextual bandits, online RL, dynamic pricing, etc.

## PredictionResult

::: ml_platform.serving.stateful.PredictionResult

## StatefulServiceBase

::: ml_platform.serving.stateful.StatefulServiceBase

## create_stateful_app

::: ml_platform.serving.stateful.create_stateful_app
